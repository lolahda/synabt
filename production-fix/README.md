# 🔧 حل مشكلة Invalid JWT - خطوات بسيطة

## ⚡ تطبيق سريع (3 دقائق)

### الخطوة 1️⃣: نسخ الملفات

انسخ الملفات التالية **بالضبط** كما هي إلى مشروعك:

```
✅ انسخ src/lib/supabase.ts → استبدل الملف الموجود
✅ انسخ src/components/auth/AuthProvider.tsx → استبدل الملف الموجود
✅ انسخ vercel.json → ضعه في جذر المشروع
✅ انسخ netlify.toml → ضعه في جذر المشروع
✅ انسخ .env.example → أعد تسميته لـ .env وأضف قيمك
```

### الخطوة 2️⃣: إعداد Supabase (الأهم!)

افتح `supabase-url-config.md` في هذا المجلد واتبع التعليمات **بالضبط**.

⚠️ **هذه الخطوة إلزامية** - بدونها لن يعمل حتى لو نسخت الملفات!

### الخطوة 3️⃣: إعداد متغيرات البيئة

#### في Vercel:
1. اذهب لمشروعك → Settings → Environment Variables
2. أضف:
   - `VITE_SUPABASE_URL` = قيمتك من Supabase
   - `VITE_SUPABASE_ANON_KEY` = قيمتك من Supabase

#### في Netlify:
1. اذهب لمشروعك → Site settings → Build & deploy → Environment
2. أضف نفس المتغيرات أعلاه

### الخطوة 4️⃣: النشر

```bash
# ارفع الملفات لـ Git
git add .
git commit -m "Fix Invalid JWT issue"
git push

# في Vercel/Netlify
# اضغط Redeploy أو Clear cache and deploy
```

---

## ✅ التحقق من الحل

بعد النشر:

1. افتح الموقع المنشور
2. افتح Console (F12)
3. يجب أن ترى:
   ```
   ✅ Supabase Config: { url: "https://...", hasKey: true }
   ```
4. سجل دخول
5. يجب أن ترى:
   ```
   🔔 Auth event: SIGNED_IN
   ✅ User signed in: your@email.com
   ```

❌ **إذا رأيت `Invalid JWT`:**
- تحقق من الخطوة 2 (Supabase URL Configuration) - غالباً هنا المشكلة
- تأكد من متغيرات البيئة صحيحة
- امسح Cache وأعد النشر

---

## 📂 الملفات في هذا المجلد

| الملف | الوصف | إلزامي؟ |
|------|-------|---------|
| `src/lib/supabase.ts` | إعدادات Supabase المُحدثة | ✅ نعم |
| `src/components/auth/AuthProvider.tsx` | معالج المصادقة المُحدث | ✅ نعم |
| `vercel.json` | إعدادات Vercel | ✅ إذا تستخدم Vercel |
| `netlify.toml` | إعدادات Netlify | ✅ إذا تستخدم Netlify |
| `.env.example` | مثال لمتغيرات البيئة | 📖 للمرجعية |
| `supabase-url-config.md` | تعليمات Supabase | ✅ نعم، اتبعها! |

---

## 🆘 إذا استمرت المشكلة

1. **تأكد 100% من Supabase URL Configuration:**
   - افتح `supabase-url-config.md`
   - اتبع كل خطوة بدقة
   - الخطأ الشائع: نسيان إضافة `/**` في نهاية الرابط

2. **تأكد من متغيرات البيئة:**
   - يجب أن تبدأ بـ `VITE_` بالضبط
   - لا مسافات أو أسطر جديدة
   - انسخ من Supabase مباشرة

3. **امسح Cache تماماً:**
   ```bash
   # في Vercel
   Deployments → ⋯ → Redeploy
   
   # في Netlify
   Deploys → Trigger deploy → Clear cache and deploy
   
   # في المتصفح
   F12 → Application → Local Storage → Clear All
   ```

---

## 🎉 النتيجة المتوقعة

بعد تطبيق هذه الملفات:
- ✅ تسجيل الدخول يعمل بدون `Invalid JWT`
- ✅ Session يستمر حتى بعد إعادة تحميل
- ✅ يعمل على Vercel, Netlify, أو أي استضافة
- ✅ نفس التجربة تماماً كما في OnSpace

**جربها الآن! 🚀**
